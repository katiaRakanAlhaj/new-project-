import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#0000ff',
      dark: 'black',
      light: 'white',
      background: '#F1F5F9',
    },
  },
});
